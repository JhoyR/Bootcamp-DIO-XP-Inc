namespace Bootcamp_DIO_XP_Inc.conteudo.SintaxeBasica
{
    public class TiposDeOperadores
    {
        /*
        *   OPERADOR DE ATRIBUIÇÃO
        *       O operador de atribuição '=' atribui o valor do operando do lado direito a uma variável, uma '=' ou um elemento indexador fornecido pelo operando do lado esquerdo. O resultado de uma expressão de atribuição é o valor atribuído a um operando do lado esquerdo. O tipo do operando do lado direito deve ser o mesmo ou implicitamente conversível para o operando do lado esquerdo. 
        *
        *
        *        
        */
    }
}